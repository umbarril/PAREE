import type { JSX } from "react";
import Base from "../components/Base";

export default function About(): JSX.Element {
    return (
        <Base>
            <h1>About Page</h1>
        </Base>
    );
}